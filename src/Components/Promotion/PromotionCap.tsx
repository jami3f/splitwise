export default function PromotionCap() {
  return <></>;
}
{
  /* <Row
names={["Promotion Cap"]}
setPeople={setPeople}
passedKey={keyCount++}
className="text-green-500"
people={[maxPromotion]}
limit={1}
handleNameClick={() => {}}
ref={(el: HTMLInputElement | null) => (inputRefs.current[1] = el)}
refocus={refocus}
>
<MaxPromotionTotal
  promotion={promotion.items[0]?.price}
  maxPromotion={maxPromotion.items[0]?.price}
  people={[...Object.values(people)]}
/>
</Row> */
}
